# DECISIONS — Prasankit

> **Append-only log กันลืม.** หนึ่งบรรทัดต่อ decision ที่ "ตกลงแล้ว": วันที่ + สรุปสั้น + ลิงก์ doc ที่ลงรายละเอียด.
> **กฎ:** เพิ่มล่างสุดเสมอ · ห้ามแก้/ลบบรรทัดเก่า · กลับ decision = เพิ่มบรรทัดใหม่ที่ระบุว่า supersede อันไหน.
> log นี้ไว้ "ไล่ timeline + กันตกหล่น" ไม่ใช่ที่เก็บเนื้อหา — เนื้อหาเต็มอยู่ใน doc ที่ลิงก์.

| # | วันที่ | Decision | รายละเอียด |
| --- | --- | --- | --- |
| D1 | 2026-05-27 | v2 = โปรดักต์เดิมของ v1 (multi-tenant PM platform) แต่ rebuild ใหม่ใต้ vibe-code loop. v1 เป็น reference ไม่ใช่ข้อผูกมัด | [01-vision §1–§2](01-vision.md) |
| D2 | 2026-05-27 | scope demo แรก **ยังไม่ล็อก** — ยกไป build-plan doc. vision เก็บแค่ north star | [01-vision §10](01-vision.md) |
| D3 | 2026-05-27 | User↔Tenant = **many membership** (1 identity อยู่ได้หลาย workspace, สร้างเอง/ถูกเชิญ). resolve tenant per session ฝั่ง backend. **ไม่ทำ cross-tenant switching UI** รอบนี้ | [01-vision §4.1, §9](01-vision.md) |
| D4 | 2026-05-27 | External client เข้าร่วม Client Project ผ่าน **invite เข้า workspace** (Org User + Project Viewer/Member) ไม่สร้าง client portal แยก | [01-vision §6.3](01-vision.md) |
| D5 | 2026-05-27 | Task status รายโปรเจคต้อง **map กับ 3 universal category** (To Do/In Progress/Done) เพื่อ rollup ข้ามโปรเจค | [01-vision §7](01-vision.md) |
| D6 | 2026-05-27 | Positioning: **ไม่ใช่ Trello/Jira/Notion** — wedge = งวดงาน+ตรวจรับ+การเงินโครงการ+lifecycle+audit. task/เอกสารทำให้ "พอใช้ดี" ไม่ "ลึกสุด" | [01-vision §1.1](01-vision.md) |
| D7 | 2026-05-27 | Task = **board+list view** (calendar/table future), การ์ดมี comment+checklist+attachment, **ไม่ทำ epic/sub-task hierarchy** รอบนี้ | [01-vision §7](01-vision.md) |
| D8 | 2026-05-27 | Document = **File Center (เก็บไฟล์)** ไม่ใช่ editor/wiki แบบ Notion | [01-vision §9](01-vision.md) |
| D9 | 2026-05-27 | ระบบกันลืมข้ามรอบ = docs (ตัวจริง) + DECISIONS.md (log นี้) + Kael memory (ป้ายบอกทาง). decision ที่ไม่ลง = ถือว่ายังไม่ตัดสิน | [00-workflow §9](00-workflow.md) |
| D10 | 2026-05-28 | เพิ่มระบบ handoff: `docs/99-handoff.md` (จุดเริ่ม session หน้า) + กฎ §9.1 — พอ User สั่งปิด session, Kael เช็ค commit/decision → อัปเดต handoff → ให้ prompt ส่งต่อ | [00-workflow §9.1](00-workflow.md), [99-handoff.md](99-handoff.md) |
| D11 | 2026-05-28 | v2 **inherit tech stack ของ v1** ทั้งชุด (Go+Fiber / Postgres18 / Redis8 / MinIO / bcrypt / UUIDv7 / hexagonal). rebuild = process/loop ไม่ใช่ tech. GORM คงไว้ก่อน (data-model doc อาจทบทวน hot path) | [02-architecture §2](02-architecture.md) |
| D12 | 2026-05-28 | Tenant isolation = **shared DB, shared schema, `tenant_id` column** ทุกตาราง (ของ v1). บังคับด้วย isolation invariant (repo รับ tenant_id เสมอ). Postgres RLS = future hardening ไม่ทำรอบนี้ | [02-architecture §4](02-architecture.md) |
| D13 | 2026-05-28 | Auth = **opaque session token** (สุ่ม 32B, เก็บ sha256 hash ใน Redis, cookie httpOnly, revocable — ไม่ใช่ JWT). Tenant resolution = **`X-Workspace-Slug` header** → backend เช็ค membership → TenantContext. ไม่เชื่อ tenant_id/role จาก client | [02-architecture §5](02-architecture.md) |
| D14 | 2026-05-28 | 02-architecture = **backend-first** (เหมือน v1 API-only). Frontend stack + วิธีคุย API **ยกไป build-plan** ตั้ง backend ให้แน่นก่อน | [02-architecture §1](02-architecture.md) |
| D15 | 2026-05-28 | **ไม่เก็บ enum ทุกกรณี** — controlled vocabulary ทุกตัว = master table + FK (ห้าม Postgres ENUM / `CHECK(IN)`). ทุก master table มี `code`(stable)+`is_system`+`sort_order`+i18n label+`status`; โค้ดอ้าง system row ผ่าน `code`. ต่างจาก v1 ที่ทำผสม | [02-architecture §7](02-architecture.md) |
| D16 | 2026-05-28 | **Authz (membership/org role/project role) resolve per-request ไม่ cache ใน session** — กัน stale authz, คง revoke-ทันที (เหตุผลเดียวกับเลือก session แทน JWT), เข้ากับ per-request multi-workspace. (cross-model review: reviewer เสนอ cache ใน SessionRecord, Kael ค้าน, User เลือก per-request) | [02-architecture §5.5](02-architecture.md) |
| D17 | 2026-05-28 | **ล็อก scope first cut (resolve D2)** — goal = proof-of-loop ภายใน. In: Foundation+Project+Team+Deliverable+ตรวจรับ+Finance(thin)+**Task board**. Out รอบนี้: Document/Announcement/Activity-Log UI. (Task board User override ใส่กลับจาก wedge-thin-slice เดิม) | [03-build-plan §2](03-build-plan.md) |
| D18 | 2026-05-28 | **FE stack = Next.js + TypeScript** (resolve D14) — responsive web, consume API ผ่าน session cookie + X-Workspace-Slug | [03-build-plan §3](03-build-plan.md) |
| D19 | 2026-05-28 | **Build approach** — 1 milestone = 1 vertical slice; **1 Sonnet dispatch = 1 stack** (BE นำ FE ครึ่งก้าว); M0 = dual-stack walking skeleton; data-model just-in-time ต่อ milestone. (operational จาก cross-model review) | [03-build-plan §4](03-build-plan.md) |
| D20 | 2026-05-28 | **Signup ไม่ auto-create workspace** — empty state → สร้าง/รับเชิญ (รองรับ multi-workspace D3). flow เต็มยกไป flow-doc. (reviewer เสนอ auto-create, User เลือก empty-state) | [03-build-plan §7](03-build-plan.md) |
| D21 | 2026-05-28 | **Pin Go 1.26** (refine D11) — greenfield ไม่ลอก minor ของ v1 (1.25) ดิบๆ, bump เป็น minor ล่าสุด ความเสี่ยงต่ำ ได้ compiler/stdlib/security ใหม่. go.mod `go 1.26.0`, Dockerfile `1.26-alpine`, `GOTOOLCHAIN=auto`. (M0 review: User เริ่ม proposal → bump ก่อน dispatch) | [02-architecture §2](02-architecture.md) |
| D22 | 2026-05-28 | **FE baseline (refine D18)** — App Router + Tailwind + **pnpm**; เรียก API แบบ client-side `fetch` (`credentials:'include'`) เพื่อพิสูจน์ CORS+cookie จริง; test lane = Vitest; อยู่ `app-web/`. (FE M0 spec: User เลือก pnpm over npm) | [03-build-plan §3](03-build-plan.md) |
| D23 | 2026-05-28 | **Host ports = high ports เลี่ยงชน** — api `18080`, web `13000` (container listen คงเดิม 8080/3000). แยก host/container ผ่าน `API_EXTERNAL_PORT`/`WEB_EXTERNAL_PORT` (เหมือน `*_EXTERNAL_PORT` ของ pg/redis/minio). `API_CORS_ALLOWED_ORIGINS`→`:13000`, `NEXT_PUBLIC_API_URL`→`:18080` ตามพอร์ต host ใหม่. (User request: M0 boot) | docker-compose.yml / .env.example |
| D24 | 2026-05-28 | **Collapse `tenant_id` → `workspace_id`** (resolve open thread 02 §4.1) — ตัด tenant_id แยกทิ้ง, `workspaces.id` = tenant boundary, ทุกตาราง workspace-scoped ถือ `workspace_id` เป็น isolation key เดียว (workspace=tenant 1:1, vision §4.1) | [04-data-model §3](04-data-model.md) |
| D25 | 2026-05-28 | **M1 auth scope = login + account-safety** — signup/login/logout (bcrypt, session Redis) + email-verification + password-reset + failed-login lockout. (User เลือกเหนือ Kael rec ที่เสนอ bare-login) | [04-data-model §4.1](04-data-model.md) |
| D26 | 2026-05-28 | **แยก account ↔ identity** — `user_accounts` (identity กลาง) + `auth_identities` (email_password ตอนนี้, OAuth-ready). (User เลือกเหนือ Kael rec single-table; OAuth/SSO ยัง non-goal vision §9) | [04-data-model §4.1](04-data-model.md) |
| D27 | 2026-05-28 | **i18n label = inline `label_th`/`label_en`** บน master table (ล็อก 2 ภาษา vision §8; ภาษาที่ 3 = migration เพิ่มคอลัมน์ YAGNI; row ระบบ FE แปลจาก `code` ได้ 02 §6) | [04-data-model §2.3](04-data-model.md) |
| D28 | 2026-05-28 | **Master FK by `code` (TEXT)** ไม่ใช่ `id` — master มี `id` UUID PK + `code` UNIQUE, FK อ้าง `code`. code immutable → predicate อ่านออก (`WHERE status_code='active'`), partial index ไม่ผูก magic-UUID, ไม่ต้อง startup cache resolve FK. (Kael เสนอ id+cache เดิม → external review รอบ 1 ชี้ pain เรื่อง tooling+magic-UUID → พลิกเป็น code, User เคาะ; Kael ไปคนละทางกับ reviewer ที่ยืนยัน id) | [04-data-model §2.4](04-data-model.md) |
| D29 | 2026-05-28 | **Log-table carve-out จาก D15** — `audit_logs`/`security_events` ถือ vocabulary (`action`/`result`/`event_type`/`severity`) เป็น machine code TEXT คุมด้วย Go const อย่างเดียว **ไม่มี FK/`CHECK`** (log append-only ปริมาณสูง + vocab โตตามฟีเจอร์). external review approve + ชี้ให้ตัด `CHECK` ออกเพื่อ consistent | [04-data-model §2.7](04-data-model.md) |
| D30 | 2026-05-28 | **Email transport = real SMTP + Mailpit dev-catcher** (เลือกเหนือ log-sender) — `email.Sender` port (pure) + stdlib `net/smtp` adapter (nil auth เมื่อไม่มี username; PlainAuth เมื่อมี); dev/test ใช้ Mailpit (compose `prasankit-mailpit`, SMTP host `11025`/UI-API `18025`); shared โดย invite (tenancy) + verify/reset (account-safety). config `MAIL_SMTP_*`/`MAIL_FROM_*`/`MAIL_INVITE_BASE_URL`. (User เลือก real SMTP) | docker-compose.yml / .env.example / `internal/modules/email` |
| D31 | 2026-05-28 | **Audit log = must-succeed in-tx** สำหรับ business mutation — `workspace.create`/`member.invite`/`invitation.accept` เขียน `audit_logs` ใน **tx เดียว** กับ insert หลัก (all-or-nothing, non-repudiation) ผ่าน `auditRepo.LogTx(tx, entry)`; ต่างจาก `security_events` (auth) ที่เป็น **best-effort**. (cross-model review เสนอ, User เคาะ) | [04-data-model §4.4](04-data-model.md) |
| D32 | 2026-05-28 | **Workspace slug = public identifier** — `requireTenantContext` คืน **404** (slug ไม่พบ) vs **403** (พบแต่ไม่ใช่สมาชิก) ไม่ทำ anti-enumeration (slug รั่วทาง create 409 อยู่แล้ว); accept-invite บังคับ **email-match** (account.primary_email = invitation.email) กัน token hijack. (cross-model review) | [02-architecture §5.2](02-architecture.md) / [04-data-model §4.3](04-data-model.md) |
| D33 | 2026-05-28 | **M1 login gating = block จนกว่า verify email** (dispatch 5a) — login ของ account `pending_verification` ถูกปฏิเสธด้วย distinct `ErrEmailNotVerified` (403 `auth.email_not_verified`) **หลัง password ผ่าน step 4** (จึงไม่เป็น enum oracle); `IsLoginAllowed()` = `active` เท่านั้น. verify confirm flip `pending_verification→active` + set `email_verified_at` (guarded token consume กัน double-confirm race; conditional flip ไม่ downgrade suspended). signup auto-ส่ง verification email (best-effort) + resend endpoint. (User เลือกเหนือ Kael rec ที่เสนอ login-allowed-while-pending) | [04-data-model §4.1](04-data-model.md) |
| D34 | 2026-05-28 | **Account-safety anti-abuse = defer** (dispatch 5a) — `/auth/verify-email/resend` คืน **204 เสมอ** (anti-enum best-effort, **ไม่ทำ timing padding**: ยอมรับ timing oracle เพราะ signup รั่ว existence ทาง 409 อยู่แล้ว — precedent D32); **rate-limit/cooldown** ของ auth endpoints (login/signup/resend/reset) ยกไป dedicated combined pass (ยังไม่มี ratelimit infra). (external review เสนอ timing-fix `FakeHashExecution` + cooldown; Kael ค้านว่า fix กลบ SMTP variance ไม่ได้ + ซ้ำซ้อนกับ 409; User เลือก defer) | [04-data-model §4.1](04-data-model.md) |
| D35 | 2026-05-28 | **M1 password-reset BE (5b)** — `/auth/password-reset/request` คืน **204 เสมอ** (anti-enum; ออก token ได้ทุก account แม้ pending). `/auth/password-reset/confirm {token,new_password}`: validate password ≥8 → guarded consume token (`RowsAffected==0`→410 กัน double-confirm race) → set `password_hash`+`password_changed_at` → **clear lockout** (`failed_login_count=0`,`locked_until=NULL`, recovery). reset **ไม่ verify email** (ไม่แตะ `account_status_code` — แยก concern, User เลือก); token TTL=1h, 1 active/identity; reuse `ErrTokenInvalid`(404)/`ErrTokenExpired`(410). **session revocation ยกไป D36/5c**. (external review รอบ 2: รับ field-fidelity→`password_changed_at` เข้า GORM model+domain; Kael ค้าน switch-to-`tx.Model` (consistent raw-SQL กับ 5a) + ค้าน 404/410-oracle (token 256-bit เดาไม่ได้)) | [04-data-model §4.1](04-data-model.md) |
| D36 | 2026-05-28 | **Lazy session revocation on password change (5c) — ปิด M1 BE** — `requireSession` → `ResolveSessionAccount`: ถ้า `session.created_at < identity.password_changed_at` → ลบ session + 401 (lazy revoke; password reset เตะทุกอุปกรณ์เก่าออกโดยไม่ต้องมี Redis account→session index). `password_changed_at == nil` → skip (no regression). consolidate `GetSession`+`GetAccountByID` → `ResolveSessionAccount` (ลบ 2 เมธอดที่ตาย). **Kael เขียนเอง (User เลือก option "ผมเขียนเอง", เลี่ยงรอบ Sonnet ที่สะดุด)**. (external review รอบ 3 over-reached: Kael ค้าน Unix-second fix ที่ผิด/`<=`สร้าง false-positive, "N+1 critical" overstated (เป็น +1 indexed read), MFA/multi-identity premature; รับ JOIN-optimization + multi-identity rethink เป็น open thread future) | [04-data-model §4.1](04-data-model.md) |
| D37 | 2026-05-30 | **Implementer role = Opus 4.8 (hybrid; refine §2 เดิมที่ Sonnet=implementer)** — budget ไม่ใช่ข้อจำกัดแล้ว → อัป implementer เป็น Opus 4.8. (a) feature/module ใหญ่ = dispatch Opus 4.8 แยก instance (role implementer) คง "คนเขียน≠คนรีวิว" (main-thread Opus=architect/reviewer); (b) งานเล็ก/iterate/debug/config = Opus เขียนตรง (ขยาย §2.1); Sonnet = option งาน trivial. Kael ค้านเขียนเอง+รีวิวเอง (confirmation bias; หลักฐาน session นี้: จับ `make dev-up` infra bug + stale-container ได้เพราะรีวิวโค้ดที่ไม่ได้เขียนเอง) → คงแยก author/reviewer สำหรับงานใหญ่. (User เลือก hybrid ข้อ 3) | [00-workflow §2](00-workflow.md) |
| D38 | 2026-05-30 | **Composite FK iron rule — amend architecture §4.3 จาก 3→4 invariants** — ทุก workspace-scoped business row ที่อ้าง "assignment/owner/member-of-something" ใช้ composite FK `(workspace_id, *)` ไปที่ parent โดยมี backing `UNIQUE (workspace_id, id)` หรือ `(workspace_id, code)`. DB = **defense-in-depth** ข้อ 4 ของ isolation invariant (เดิม 3 ข้อพึ่ง service-discipline). **Scope exclusion:** audit/identity columns (created_by, updated_by, deleted_by, invited_by, owner_user_account_id ที่ workspace-boundary) ใช้ single-column FK → `user_accounts(id)` เพราะ actor เป็น global identity (D3 multi-ws). M2 apply: projects.owner, project_members.(project+membership), project_member_positions, workspace_memberships.company_position. M3+: task_assignee, budget_approver, deliverable_reviewer | [04 §M2.2 (2.8)](04-data-model.md) + [02 §4.3](02-architecture.md) |
| D39 | 2026-05-30 | **Positions = 2 ตารางแยก + FK-by-code consistent + junction append/delete-only** — `project_positions` (M:N ผ่าน `project_member_positions`, 1 member สวมหลาย hat) + `company_positions` (1:1 nullable column บน `workspace_memberships`, M2 trade-off). 2 ตารางป้องกัน category leak ที่ table เดียว+category_code จะเปิด. **FK strategy consistent** ทั้ง 2 customizable masters = FK-by-code ขนาน D28 (§M2.2.4); code immutable + no-reuse-after-deprecate ทำให้ stable เท่า id (§2.9). **`project_member_positions` junction = append/delete-only** (ไม่มี `updated_at`/`updated_by`; history อยู่ใน `audit_logs`) — ขนาน M1 token tables. **External review #1 disagreement (recorded):** reviewer เสนอเพิ่ม `status='active'|'revoked'` + `updated_at`/`updated_by` บน junction (เพื่อ row-level history). Kael ค้านว่าขัด §2.6 (single-source-of-truth: timestamp/status enum ไม่เก็บซ้อนกัน) — status column + audit_logs = dual source ที่ drift inevitable; audit_logs เป็น proper history mechanism อยู่แล้ว ไม่สิ้นเปลือง; ถ้า future compliance ต้องการ "as of timestamp" query → `project_member_role_history` table แยก (§M2.8). **User เลือกตาม Kael — คงเดิม.** | [04 §M2.3.2, §M2.3.4, §M2.3.5](04-data-model.md) |
| D40 | 2026-05-30 | **Project owner = column `projects.owner_project_member_id` composite IMMEDIATE FK** ไป `project_members(workspace_id, id)`. ขนาน `workspaces.owner_user_account_id` (M1 §4.3). O(1) ownership lookup; single-owner ที่ column-level. **Standard 3-step create sequence** (INSERT projects NULL owner → INSERT project_members → UPDATE projects SET owner) ทำงานได้กับ IMMEDIATE เพราะ column nullable. Service-layer guard 3 ข้อ (ws-match จาก composite FK / role-match `project_owner` / `removed_at IS NULL`) + role-drift guard ห้าม UPDATE project_role_code ของ owner row นอก ownership-transfer service. **External review #1 refinement (history):** workflow candidate ใช้ `DEFERRABLE INITIALLY DEFERRED` รองรับ chicken-and-egg ที่ COMMIT-time. Reviewer ทักว่า DEFERRABLE เพิ่ม complexity + claim deadlock risk (เหตุผล deadlock อ้าง "Postgres internal execution plan swap" ของ DML — ไม่ตรง Postgres semantics: Postgres ไม่ reorder DML statements ใน tx; INSERT row ใหม่ไม่ block cross-tx). Reviewer เสนอ swap INSERT order (member ก่อน project) — ใช้ไม่ได้กับ composite FK iron rule (`project_members.(workspace_id, project_id) → projects` IMMEDIATE จะ fail ทันที). **Underlying concern (DEFERRABLE เพิ่ม complexity) ถูก** → Kael พลิกเป็น IMMEDIATE + nullable + standard sequence: ทำงานได้โดยไม่ต้อง DEFERRABLE feature, ตรงเจตนา reviewer ที่จะเลี่ยง DEFERRABLE | [04 §M2.3.3](04-data-model.md) |
| D41 | 2026-05-30 | **Company position = ALTER `workspace_memberships`** (nullable `company_position_code` + composite FK ไป `company_positions(workspace_id, code)`, FK-by-code §M2.2.4). **Cardinality 1:1 nullable** ตาม vision §6 ตัวอย่าง singular ("Infrastructure Manager") — trade-off explicit: ถ้าจริงๆ multi-position (Engineering Manager + Acting Head) → migration upgrade เป็น join table + API คืน array (FE handle 0/1/N เสมอ, no break). M3+ พิจารณาตามหลักฐาน | [04 §M2.3.5](04-data-model.md) |
| D42 | 2026-05-30 | **Project slug = optional public identifier per workspace + cross-ws 404 collapse** (refine D32 สำหรับ project). CITEXT nullable, UNIQUE per ws among non-deleted, format pattern เดียวกับ workspace slug. **Cross-workspace probe collapse 403→404:** member ของ ws A probe `/wsB/projects/{guess}` → return 404 เสมอ (ไม่แยก "ไม่มี" vs "มีแต่ไม่ใช่สมาชิก ws B") กัน enumerate project name ของ ws B. ภายใน ws ที่ตัวเป็นสมาชิก → 404 vs 403 แยกตามปกติ. D32 email-match clause ไม่ extend มา project ใน M2 (ws-invite-accept บังคับ email-match แล้ว) | [04 §M2.2 (2.10), §M2.3.3](04-data-model.md) |
| D43 | 2026-05-30 | **`audit_logs.project_id` ALTER policy** — ปิด M1 §8 open thread. nullable + partial index `WHERE project_id IS NOT NULL` + **`CHECK (project_id IS NULL OR workspace_id IS NOT NULL)`** (structural, อยู่นอก D29 carve-out ที่ห้าม CHECK เฉพาะ vocabulary columns). **NO FK to `projects(id)`** เพราะ audit append-only ต้อง outlive resource deletion + กัน circular dep migration (pattern M1 `resource_id`). **Compensating discipline (NEW invariant #6 §M2.5):** write path `LogTx` รับ workspaceID+projectID แยก param; read path WHERE workspace_id + project_id พร้อมกัน; PR review P0 block `WHERE project_id` เดี่ยว. **External review #1 disagreement (recorded):** reviewer เสนอ runtime existence-check ใน `LogTx` (SELECT projects ก่อนบันทึก audit) เพื่อกัน "orphaned log injection". Kael ค้านว่า threat model misaligned (`LogTx` = internal repo, ไม่ exposed ต่อ client; ถ้า attacker เรียกได้ตรง = code execution อยู่แล้ว, FK ไม่ช่วย) + fix เพิ่ม +1 SELECT ทุก audit-write (hot-path overhead) + ซ้ำซ้อน service-layer validation; "FE crash" ไม่ realistic (JOIN-miss → "Unknown project" graceful fallback คือ pattern audit-outlive-resource ปกติ). read invariant + FE graceful-handle ครอบ scenario จริงพอ. **User เลือกตาม Kael — คงเดิม.** | [04 §M2.3.5, §M2.5, §M2.6 step 9](04-data-model.md) |
| D44 | 2026-05-30 | **Project lifecycle = free transitions ใน M2** (no state machine, no DB CHECK, no transition table) — FK บังคับแค่ vocabulary; ทุก status change เขียน `audit_logs`. confirm-only ตาม build-plan §5 row M2. M3+ อาจเพิ่ม transition guard ที่ **service layer** (ไม่ใช่ DB CHECK) ถ้า product พบ misuse บ่อย — review ตอน deliverable/finance entity มาผูก lifecycle | [04 §M2.1, §M2.3.1, §M2.3.3](04-data-model.md) |
| D45 | 2026-05-31 | **Split 6b → 6b-1 (team core) + 6b-2 (positions)** — แบ่ง M2 BE ครึ่งหลังเป็น 2 dispatch: **6b-1** = `project_members` + owner composite FK + create owner-3-step (migrations 000012-000013, committed); **6b-2** = position masters (`project_positions`/`company_positions`) + `project_member_positions` junction + `ws_memberships.company_position_code` (migrations 000014-000016, planned). เหตุผล: 6b-1 เป็นส่วนเสี่ยงสูง/contract-critical (owner 3-step, composite FK iron rule, แตะโค้ด 6a) → review diff เล็กโฟกัส (trust-but-verify), FE team-mgmt ปลดล็อกหลัง 6b-1, migrations FK-ordered + contiguous ต่อ sub-slice. renumber จาก §M2.6 sketch เดิม (Pin A อนุญาต patch เลขตอน merge). (User เลือก split เหนือ single-dispatch) | [04 §M2.6](04-data-model.md) |
| D46 | 2026-05-31 | **6b-1 owner-read = option 1 (inject `project.ProjectRepository` เข้า `projectmember.Service`)** — role-drift / owner-removal guard ใน `projectmember.Service` อ่าน `projects.owner_project_member_id` ผ่าน injected `project.ProjectRepository.FindByIDForWorkspace` (3-param `NewService`). edge `projectmember → project` สะอาด acyclic — `project` import อะไรจาก `projectmember` ไม่ได้ (ใช้ `project.OwnerMemberSeed` primitives + inline literal `"project_owner"` กัน import cycle; owner member row เขียนผ่าน file-private `ownerMemberRow` ที่มี `TableName()`). option 2 (เพิ่ม method อ่านตาราง `projects` ใน projectmember adapter) = layering smell แย่กว่า → reject. external cross-model review #2 (grounded) validate design นี้. (User เคาะ option 1) | [04 §M2.3.4](04-data-model.md) |
| D47 | 2026-05-31 | **6b-2-BE positions build decisions (5 OQ resolutions + external-review reconcile) — ปิด M2 BE** — slice = position masters CRUD + junction + ws company_position; migrations **000014-000016 committed** (ปิด D45 §M2.6 renumber). **(OQ-1)** 2 separate modules `projectposition`/`companyposition` (ไม่ใช่ 1 module + `kind` discriminator; D39 anti-category-leak ที่ระดับ module + companyposition มี ws-attach surface พิเศษ). **(OQ-2)** เพิ่ม read-only additive `projectmember.FindByID` (includes-removed) — จุดเดียวที่แตะโค้ด 6b-1 — เพื่อ honor §M2.5(c) 422-for-removed (`FindActiveByID` collapses removed→nil→404 แยกไม่ออก). **(OQ-3)** ws company_position write อยู่ใน `companyposition` module เขียน `workspace_memberships` ผ่าน file-private `membershipRow`+`TableName()` (precedent `ownerMemberRow`/D46), ไม่ import `workspacedbrepo`, ไม่แตะ `workspace.Membership` domain. **(OQ-4)** single audit action `membership.company_position_change` (old/new value, `resource_type=workspace_membership`) ขนาน `project_member.role_change` — ไม่ใช่ set/clear แยก, เลี่ยงชน namespace `company_position.*` (master CRUD). **(OQ-5)** default endpoints: `POST /:code/deprecate`, junction nested `/projects/:id/members/:memberId/positions`, `PUT /memberships/:id/company-position` (nullable body). **External cross-model review reconcile (reviewer ณัชชา, ผ่าน file-confirmation gate):** Finding-1 HIGH "bare UNIQUE INDEX ไม่ใช่ valid composite-FK target → CI พัง" **OVERTURNED** — empirically false (เทส PG: non-partial unique index เป็น FK target ที่ valid; เฉพาะ partial index ที่ถูก reject); ยังเปลี่ยนเป็น `ADD CONSTRAINT ... UNIQUE` เพื่อ convention-consistency (000008/010/012) ไม่ใช่เพราะ bug (User confirm overturn). Finding-2 MED unassign member-gate → mandatory (รับ; เหตุผลจริง = audit `project_id` integrity เพราะ junction DELETE bind project_id เองไม่ได้). Finding-3 LOW (test-name) → ค้าน (non-issue). Implementer (Opus แยก instance, D37) crash-tail (junction `repository_test.go` หาย + goose NO-TRANSACTION token ใน prose comment ของ 000014) recovered. trust-but-verify: `go test -p 1 -count=1` 79 integration PASS/0 skip บน PG18, goose down/up round-trip, live e2e smoke (CRUD + guards 422/409 + audit project_id-NULL + single action ×2). **M2 BE complete (6a+6b-1+6b-2).** | [04 §M2.3.2/.3.4/.3.5/.6](04-data-model.md) |
| D48 | 2026-05-31 | **M2 FE-A projects-core build decisions (6 OQ = defaults + external cross-model review reconcile)** — FE-A = projects list/create/detail/edit/status-change/soft-delete (app-web). **User accept 6 OQ defaults:** (1) hardcode status/type labels (pin migration 000009, ไม่เพิ่ม BE masters endpoint); (2) single `[id]` route + inline edit; (3) RBAC hide non-owner/admin; (4) inline status Select + ConfirmDialog; (5) FE-cast `err.details.fields` (project handler emit OBJECT `{fields}`/`{field}` ต่างจาก M1 bare array — ไม่แก้ BE); (6) slug OPTIONAL. **External review (reviewer ณัชชา) — Kael overturn 2 HIGH ด้วยหลักฐาน source/PG จริง (ไม่ rubber-stamp):** UNIQUE-INDEX-FK-target (6b-2) = false (non-partial unique index เป็น composite-FK target ที่ valid); PUT-omit-can't-clear (FE-A) = false (repo `Updates(map[string]any)` = full-replace, omit→nil→NULL clears). รับ MED (mandatory unassign gate / apiPut+apiDelete via parseBody / validateDateRange gate). | [03 §5](03-build-plan.md) (spec-fea-projects.DRAFT deleted at merge) |
| D49 | 2026-05-31 | **FE-A manual-smoke findings (M1 fixes + §M2.4 label alignment)** — manual click-through ของ User จับสิ่งที่ automated (vitest 160/build/serve-200) จับไม่ได้: (1) **[BE bug] CORS AllowHeaders ขาด `X-Workspace-Slug`** → browser block ทุก workspace-scoped request (getCurrentWorkspace + FE-A ทั้งหมด) — แก้ `cors.go` + regression test; (2) dark-mode input "จม" → ลบ create-next-app dark block + `color-scheme:light`; (3) signup confirm-password; (4) workspace contact_email pre-fill จาก primary_email; (5) home/workspaces IA redesign (projects = primary CTA; invite/create = collapsed secondary). + **§M2.4 seed-label aligned to deployed migration 000009** (doc-drift: ร่าง/เสนองาน/ดำเนินงาน → ฉบับร่าง/เสนอราคา/กำลังดำเนินการ; migration = source of truth per D27). **บทเรียน: manual smoke จับ cross-origin/visual bug ที่ automated ไม่ครอบ — ทำทุก FE slice.** Next = login→work click-reduction IA. | [02 §5](02-architecture.md) / [04 §M2.4](04-data-model.md) |
| D50 | 2026-05-31 | **FE UX redesign — slice-1 (Trello/Bruno/GitHub benchmark)** — manual smoke พบ FE "งง" (User: designer ยังงง → user งง). benchmark workflow (5 agents: web-research 3 product + อ่าน FE เรา + synthesize) พบ 3 affordance ที่ Trello/Bruno/GitHub มีเหมือนกันแต่เราขาด: (1) **switcher ที่เห็นชัด** (2) **ปุ่ม Create ถาวร** (3) **orientation chrome (อยู่ไหน)**. **slice-1 (commit 7f1d32e):** workspace-name **dropdown switcher** บน header (list + ✓active + 1-click switch + "+New workspace") · **"+New project" button + create modal** (เลิกฟอร์มแปะท้าย list) · project **cards + status-badge สี** + dates + owner · **breadcrumb** บน detail · **empty-state CTAs** (member ไม่ตัน) · 4 headless ui primitive (Dropdown/Modal[refactor ConfirmDialog ผ่านมัน]/Badge/Breadcrumb, ไม่เพิ่ม dep). **IA choice:** top-nav + header dropdown (แบบ GitHub) ไม่ใช่ left sidebar (Trello) — เบากว่า, ไม่ rework layout. **Bigger (ทีหลัง):** sidebar · card-grid+density · search/sort (ต้อง BE) · workspace landing page. **Roadmap:** project detail = hub ที่จะโตเป็น tabs/sections — FE-B(ทีม) เติม section แรก, FE-C(positions), M3+(deliverable/task/finance). UX-polish slice ข้าม external review (design validate ด้วย benchmark; review จริง = User manual smoke). | [03 §5](03-build-plan.md) (spec-fe-ux-slice1.DRAFT deleted at merge) |
| D51 | 2026-06-01 | **M2 FE-B team management build (6 OQ = defaults + new read-only BE endpoint) — section แรกในหน้า project detail** — ตอบ "สร้างโครงการแล้วไปไหนต่อ" (D50 roadmap: detail = hub). **NEW BE endpoint** `GET /api/v1/workspaces/members` (endpoint เดียวที่เพิ่มของ slice): repo JOIN `user_accounts` เอา display_name, isolation `wm.workspace_id=?`, active-only, **gated owner/admin** (perm เดียวกับ invite). 2 contract decision (จาก verifier LOW findings = shipped contract): (a) gate owner/admin **ไม่ใช่** all-4-roles ที่ DRAFT ร่างไว้ — least-privilege, picker เปิดเฉพาะ `canMutate` อยู่แล้ว; (b) response 3 fields `{workspace_membership_id, display_name, org_role_code}` ตัด `membership_status_code` (active-only filter ที่ repo แล้ว, FE type ตรง). **FE Team section** (`projects/[id]/page.tsx`): members list (ชื่อ + project-role Badge) · owner row resolve UUID→display_name in-place + role-locked/remove-disabled (**D40 owner_immutable**) · change-role inline Select (no confirm, per-row saving guard) · remove ConfirmDialog · **add-member Modal** (picker จาก endpoint ใหม่ + eligible-filter ตัดคนที่อยู่แล้ว + role Select ตัด owner + empty→invite CTA) · reinstate **invite Modal** (owner/admin). lib ใหม่: `member-api.ts` (project_members 6b-1: list/add/change-role/remove) + `workspace-api.listWorkspaceMembers` (endpoint ใหม่) + `project-roles.ts` (labels pin migration 000009). RBAC: list=all, mutate=owner/admin (hide). **6 OQ = defaults** (Modal invite · inline role-change · resolve-owner-name in-place · viewer empty copy · `team.*` i18n key · approve BE endpoint). Dispatch = **pipeline workflow** (BE impl → FE impl → 3 verifiers, author≠reviewer **D37**). **0 HIGH / 0 MED, 5 LOW** non-blocking (2 = pre-existing parallel `go test ./...` flake ที่ `make test -p1` ไม่โดน; 2 = DRAFT doc-drift; 1 = test-fake nit ที่ repo integration test คุมแล้ว). trust-but-verify (Kael): `make test -p1` เขียว (repo+handler test ใหม่ run บน PG18 จริง) · FE 185 tests + lint + build · live smoke + **User manual smoke (เพิ่ม bob@ เข้าโครงการสำเร็จ)**. | [03 §5](03-build-plan.md) (spec-feb-team.DRAFT deleted at merge) |
| D52 | 2026-06-01 | **Language toggle — persist + segmented** (เจอตอน FE-B manual smoke) — bug: สลับภาษาแล้ว refresh ไม่จำ (`lang-context` เก็บ React state ล้วน ไม่แตะ localStorage) + ปุ่ม single action-label (โชว์ "EN" ตอนอยู่ TH) อ่านสับสน. **fix:** (1) `lang-context` persist `localStorage["prasankit_lang"]` — hydrate-after-mount (default = DEFAULT_LANG ตอน SSR → อ่าน storage ใน `useEffect` หลัง mount) เลี่ยง hydration mismatch, มิเรอร์ activeSlug pattern (workspace-context); expose `setLang`. (2) `site-header` = **segmented `[ ไทย | EN ]`** (เห็น 2 ป้ายพร้อมกัน, active ไฮไลต์พื้นเข้ม) แทน action-label เดียว; ลบ i18n key `lang.toggle` ที่ไม่ใช้แล้ว. lint/build/185 tests เขียว. UX-polish slice — ข้าม external review (validate = User manual smoke). | [02 §5](02-architecture.md) |
| D53 | 2026-06-05 | **M2 FE-C slicing + FE-C1 ship (positions masters)** — แตก FE-C → **FE-C1** (masters CRUD project/company บนหน้า settings ใหม่ `/settings/positions`) + **FE-C2** (assign ตำแหน่งให้สมาชิกบน project detail) ตามแบบ D45 (diff เล็ก review ง่าย). **company position** แก้ inline บน member row (ws-level 1:1, ใส่ hint ว่ากระทบทุก project). **Decision-A**: เพิ่ม read field **additive** `company_position_code` บน project members list (`projectmember.ListByProject` JOIN `workspace_memberships` อยู่แล้ว — ~5 บรรทัด, non-breaking) ให้ company Select โชว์ค่าปัจจุบัน → dispatch ก่อน FE-C2. **Implementer = main-thread Opus** (User ปฏิเสธ sub-agent dispatch → Kael เขียนเอง; รอบนี้ author=reviewer ชดเชยด้วย **/scrutinize** + automated). **FE-C1 ship (commit fdee34f):** masters เริ่มว่าง (ไม่ seed) → FE **fetch สด ไม่ hardcode** code; `position-api.ts` kind-parametrized client (list/create/update/deprecate); RBAC read-only non-owner/admin; scrutinize verify `FieldError` json tag `{field,message}` + description clear-on-omit (service.go:378-381) ตรง BE จริง + fix `description` field-error mapping. tsc/lint/**223 tests**/build เขียว; live ใน container ที่ rebuild. FE-C2 + Decision-A BE = ถัดไป. (note: ยังไม่ได้ User manual smoke — commit ตามคำสั่ง) | [03 §5](03-build-plan.md) |
| D54 | 2026-06-05 | **Theme refresh = Teal (token-level, ทั้งแอป)** — User: แอป "โล่ง/ไม่สวย". ใช้ skill **ui-ux-pro-max** (palette/font DB) เลือกทิศ — เลี่ยง indigo/purple โหล → **Teal**. Tailwind v4 `@theme` tokens (globals.css): brand teal `#0d9488` (accent เดียว: ปุ่ม primary + focus ring + wordmark "Prasankit"), neutral **zinc→slate** (sweep 143 จุด/12 ไฟล์), canvas tint **slate-50** (การ์ดขาว "ลอย" แก้อาการโล่ง). fix bug ฟอนต์ **Arial override** ([globals.css เดิม]) → **Plus Jakarta Sans** (ละติน) + **Noto Sans Thai** (ไทย) via `next/font` (bilingual stack ละตินก่อน-ไทย fallback per-glyph). a11y focus-visible rings. **re-theme = แก้ 5 ค่าใน globals.css จุดเดียว**. skill **frontend-design** = เอาแค่หลักการ anti-slop (token/แบรนด์เป็นของ NUT-LMS คนละโปรเจกต์, stack Tailwind v3 ต่างกัน). commit f557402; tsc/lint/223 tests/build เขียว + container rebuild live. UX-polish slice — ข้าม external review (validate = User ดูของจริง). | [02 §5](02-architecture.md) |
| D55 | 2026-06-05 | **M2 FE ปิดครบ (FE-C2 + Decision-A + 3 post-smoke fixes) — M2 ทั้ง milestone done → ขึ้น M3** — **FE-C2 (commit 5062ea9):** section "ตำแหน่งของสมาชิก" บน project detail (**แยก component `PositionsSection`** — User เลือก option A): assign/unassign project-position M:N chips (picker active-only ตัดที่มีแล้ว) + company-position Select inline (ws-level 1:1, ค่าจาก Decision-A) + hint; empty-state ลิงก์ `/settings/positions` (gate canMutate); RBAC read-only; **optimistic company value** (ไม่เด้งกลับตอน save) + ไม่ blink ตอน post-mutate refetch (scrutinize จับ 2 อันนี้). **Decision-A (commit 4e1e388):** additive read field `company_position_code` บน `projectmember.ListByProject` (JOIN `wm` อยู่แล้ว, omitempty, non-breaking) + integration test (nil-default + returns-when-set); BE `make test` green. **3 fix หลัง User manual smoke** (ย้ำบทเรียน D49 — smoke จับสิ่งที่ automated ไม่จับ): (1) **nav-leak** (a12a373) — header nav (switcher/Projects/Settings) gate ด้วย `activeSlug` ล้วน + activeSlug persist localStorage → leak โผล่บน /login ตอน anonymous; fix = gate `status==="authenticated"` ด้วย (เก็บ persist ไว้ resume). pre-existing ตั้งแต่ FE-B; FE-C1 เอา Settings ไปต่อแถว + theme ทำให้เห็นชัด; debug-mantra falsify H2 (authed→redirect) ยืนยัน H1; (2) **password show/hide** (3dea31b) — `PasswordInput` reusable (eye/eye-off SVG ไม่ใช่ emoji, aria 2 ภาษา) บน login; (3) **dev-seeder + `make seed`** (36fade1). **บทเรียนใหม่ (→ memory/handoff): `make test` ใช้ DB:15433 เดียวกับแอป แล้ว TRUNCATE → ล้าง seed dev (owner@/bob@/ws) ทุกครั้ง → ต้อง `cd app-api && make seed` ตามทุกครั้ง** (`cmd/seed` กู้คืนผ่าน repo จริง: active + bcrypt demopass123). trust-but-verify: FE tsc/lint/**247 tests**/build เขียว + scrutinize + **User manual smoke ผ่าน (theme+FE-C1+FE-C2+fixes)**. **M2 ปิด (BE 6a/6b-1/6b-2 + FE-A/B/C1/C2 + Teal theme) → M3 = Deliverable + ตรวจรับ (wedge ½).** ⚠️ process: FE-C1/C2/fixes ทั้งหมด implementer = main-thread Opus (User ปฏิเสธ sub-agent → author=reviewer, ชดเชย /scrutinize) — ต่าง D37, User override. | [03 §5](03-build-plan.md) |
